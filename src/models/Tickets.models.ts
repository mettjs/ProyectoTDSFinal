export default interface Tickets {
  iDTicket: number;
  nombre: string;
  correo: string;
  pelicula: string;
  fecha: string;
  total: number;
  comboID: number;
  codigo: string;
  peliculaID: number;
}